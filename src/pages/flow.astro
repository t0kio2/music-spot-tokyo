---
import Layout from "../layouts/Layout.astro";
import { getPageBySlug } from "../lib/microcms";

const page = await getPageBySlug("follow");
---

<Layout title={page?.title ?? "参加までの流れ"}>
	<div class="container prose" set:html={page?.content ?? ""} />
</Layout>

<style>
	.container {
		padding-top: 1rem;
		padding-bottom: 3rem;
	}
</style>
